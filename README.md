# عنك — نسخة بدون Firebase

هذه نسخة **صفحة واحدة** ثابتة بالكامل (HTML/CSS/JS فقط) بدون Firebase وبدون تسجيل/حسابات/لوحة مالك.

## التشغيل
- افتح `index.html` مباشرة في المتصفح.

## التعديل السريع
- بيانات الفنيين: داخل `app.js` (المصفوفة `workers`)
- ألوان وتصميم: داخل `styles.css`
